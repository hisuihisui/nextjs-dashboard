/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //   // true: すべてのページでPPRが使える
  //   // incremental: 特定のページでPPRが使える
  //   ppr: 'incremental',
  // },
};

module.exports = nextConfig;
